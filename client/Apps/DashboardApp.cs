namespace SmartEnergyExpert.Client.Apps;

[App(icon: Icons.LayoutDashboard, title: "Панель", searchHints: ["панель", "огляд", "що порівнюється", "датасети", "результат", "допомога"])]
public sealed class DashboardApp : ViewBase
{
    public override object? Build()
    {
        return Layout.Vertical().Gap(3)
               | Text.H1("Гідроакустичне порівняння: для чого це вам")
               | Text.P(
                   "Застосунок допомагає зіставити вихід моделі (що ви очікуєте від симулятора у воді) з реальними вимірами з експерименту або запису. "
                   + "Так видно, де модель збігається з даними, де розходиться сильніше, і які гіпотези про причини розбіжностей система сформулює на основі правил — як опора для аналізу.")

               | new Card(
                   Layout.Vertical().Gap(2)
                   | Text.H3("Що саме порівнюється")
                   | Text.P(
                       "Порівнюються дві гілки даних про один і той самий предмет — акустичний сигнал у воді — але з різного джерела:")
                   | Text.Block("• Симуляція — ряд часових точок із модельною оцінкою рівня тиску (SPL) по смугах частот і глибині/дальності тощо.")
                   | Text.Block("• Поле — ряд тих самих по змісту величин, але з фактичного запису (сенсор, експедиція, стенд, довгий CSV з польового обладнання).")
                   | Text.Muted(
                       "Система намагається спарувати відповідні моменти часу та смуги частот між двома гілками й порахувати різницю в децибелах і у відносних відсотках. "
                       + "Якщо записи з різних експериментів або з різною щільністю точок, зіставлення стає складнішим — тоді варто свідомо готувати пару «симуляція + те саме поле», зокрема через режим узгодження сітки в розділі середовищної симуляції."))

               | new Card(
                   Layout.Vertical().Gap(2)
                   | Text.H3("Що ви подаєте на вхід (які датасети)")
                   | Text.P("На порівняння потрібні два датасети одночасно — по одному з кожного типу:")
                   | Text.H4("1) Датасет «симуляція» (модель)")
                   | Text.Block(
                       "Це гілка моделі: або файл CSV, який ви імпортуєте як симуляцію, або новий синтетичний набір, який будується в розділі «Середовищна симуляція» з параметрів води, дна та тривалості (за бажання — з прив’язкою до часової сітки обраного польового датасету).")
                   | Text.H4("2) Датасет «поле» (виміри)")
                   | Text.Block(
                       "Це гілка вимірів: зазвичай імпортований CSV з реального запису. У ньому ті самі логічні стовпці, що й для акустичних рядів у проєкті (час, частота, амплітуда, глибина тощо — як у поставлених зразках даних).")
                   | Text.Muted(
                       "Один і той самий фізичний запис не можна одночасно позначити й симуляцією, й полем для пари порівняння — система чекає різні набори: модель проти виміру."))

               | new Card(
                   Layout.Vertical().Gap(2)
                   | Text.H3("Що ви отримуєте на виході")
                   | Text.P("Після запуску порівняння в розділі «Гідроакустичне порівняння» ви бачите зведення по тих точках, які вдалося зіставити:")
                   | Text.Block("• Короткий підсумок — чи в цілому модель близька до поля на цьому прогоні, чи є помітні відхилення.")
                   | Text.Block("• Числові метрики (зокрема середня та «хвостова» помилка в дБ, відносна помилка) — для об’єктивної оцінки масштабу розбіжності.")
                   | Text.Block("• Списки найбільших відмінностей і часові кластери — де саме у часі та частоті найбільші розходження.")
                   | Text.Block("• Рекомендації — структуровані підказки (категорія, впевненість, аргументи); текст може бути англійською, якщо так налаштований бекенд.")
                   | Text.Block("• Графіки — наочно: метрики, накладання кривих симуляції та поля, «гарячі» ділянки за частотою та часом.")
                   | Text.Muted(
                       "Параметр «скільки найбільших відхилень деталізувати» ви задаєте перед запуском — він керує глибиною списку викидів, а не самим фактом порівняння."))

               | new Separator()
               | Text.H3("У якій послідовності користуватися розділами")
               | Text.Block("1. Керування датасетами — завантажити польовий CSV (і за потреби CSV симуляції) або прибрати зайві набори.")
               | Text.Block("2. Середовищна симуляція — за потреби отримати модельний ряд без ручного CSV або підігнати симуляцію під сітку того ж поля, з яким будете порівнювати.")
               | Text.Block("3. Гідроакустичне порівняння — оновити список, переглянути короткий опис сигналу по кожній гілці, обрати симуляцію та поле, запустити порівняння й прочитати метрики, відмінності та рекомендації.");
    }
}
